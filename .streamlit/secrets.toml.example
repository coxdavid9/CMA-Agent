# Add your key in Streamlit Cloud -> Settings -> Secrets.
# Do NOT commit a real key to GitHub.
OPENAI_API_KEY = "paste-your-openai-api-key-here"

# Optional: set these later if you add a persistent cloud database.
# CMA_DB_PATH = "data/study.db"
# CMA_AGENT_MEMORY_DB = "data/agent_memory.db"
